import 'package:flutter/material.dart';

class PermitTrackerScreen extends StatelessWidget {
  const PermitTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
